import 'package:flutter/material.dart';
import 'app.dart';

void main() {
  // Ensure Flutter engine is fully initialized before running
  WidgetsFlutterBinding.ensureInitialized();
  
  runApp(const BabyStoreApp());
}